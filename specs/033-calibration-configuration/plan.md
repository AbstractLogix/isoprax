# Implementation Plan

1. Add keyword configuration arguments using the existing conformance defaults.
2. Thread them into per-family and pooled calibration calculations.
3. Preserve the values on `CrossFamilyReport` and add a caller-level test.
