# Data Model: Evaluation Diagnostic Input Integrity

Score sequences are finite probabilities in `[0, 1]`; outcome sequences are
binary and length-matched. Reliability bins use a positive integer count, and
time-sliced train fractions are strictly between zero and one.
