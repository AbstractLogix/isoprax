# Convergence: Complete Stage 1 Public-Data Validation

## Evidence obtained

- ApacheJIT Zenodo source identity passed for `apachejit_total.csv` using the
  published MD5 and a recorded SHA-256; Apache Ignite was evaluated as one
  single Change-family system.
- Google Cluster Trace v1 source identity passed using the published SHA-1 and
  a recorded SHA-256; repeated ParentID observations were aggregated as one
  single Operational-family system.
- Both family corpora passed all Stage 1 admission gates, including lineage,
  score-time fields, chronological splits, calibration evidence, provenance,
  adequacy, and single-system boundaries.
- Change-family calibration gate: 1,750 events, ECE 0.0443, AUC 0.8434.
- Operational-family calibration gate: 556 events, ECE 0.0429, AUC 0.7522.
- Checked-in report identity: `sha256:044fced4d3f55d043e53ad494758ec20481a33c465acdccd934515fa17a285b5`.

## Verification

- `uv run pytest -q`: **221 passed**.
- Branch-aware total coverage: **97.45%**.
- Per-module coverage gate: passed at **95% minimum**; the new adapter module
  reached **100%**.
- Ruff, format check, and `git diff --check`: passed.

## Claim boundary

Stage 1 is now complete as an executed, reproducible public-data structural
validation. The result remains per-family evidence only. ApacheJIT's SZZ-style
repository label and Google Trace's resource-threshold label use different
observation processes, so cross-family pooling and Semantic/Full Conformance
remain withheld. Raw source files remain external and are not committed.

## Reproduction limitation

The report can be regenerated by supplying the two declared source files using
the quickstart command. CI intentionally does not download external datasets;
missing or changed source bytes fail closed.
