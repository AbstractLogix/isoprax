# Research

- Supplied snapshots rather than network clients preserve the offline core.
- Missing public CI evidence is unavailable, never a fabricated build result.
- Unsafe references are rejected, never sanitized into misleading provenance.
