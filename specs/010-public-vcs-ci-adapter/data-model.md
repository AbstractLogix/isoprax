# Data Model

`PublicEvidenceSnapshot`: system, immutable revision, public source reference,
optional public CI reference, timestamp, and provenance flags.

`PublicEvidenceRecord`: deterministic identity, safe references, availability
state/reason, and fixed candidate/build-preparation-only scope.
