# Tasks

- [x] Add the direct RFC 8785 dependency.
- [x] Replace `json.dumps(default=str)` in the identity helper.
- [x] Cover Unicode, number normalization, unsupported values, and safe ints.
- [x] Re-run all identity consumers and the full quality gate.
