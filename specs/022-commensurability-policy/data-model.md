# Data Model: Unified Commensurability Policy

`CommensurabilityResult` remains the single policy result. `commensurable`
describes direct/attested semantic matching; `pooling_allowed` describes the
explicitly authorized metric-pooling path, including bridgeable definitions
when retained observations are declared.
