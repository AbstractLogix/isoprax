# Quickstart: Release Metadata

Run the local check for the current release:

```bash
python scripts/check_release_metadata.py --tag v0.2.0
```

The same check runs automatically for tags matching `vX.Y.Z` in GitHub
Actions.
