# Convergence

The repository version is `0.2.0`, and its changelog entry records the
Sigstore/Rekor anchor gate, version-aware SQLite identity, and RFC 8785 hash
boundary. A release workflow validates `vX.Y.Z` tags against the package and
requires a non-empty descriptive changelog entry.
