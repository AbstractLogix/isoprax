# Convergence

The module coverage command now derives expected production modules from
`isoprax/*.py`, normalizes coverage paths, reports missing entries, and then
applies the original branch-aware minimum to every reported module.
