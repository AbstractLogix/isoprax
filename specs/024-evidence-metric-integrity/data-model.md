# Data Model: Evidence Metric Integrity

`PoolingHarmEvidence.per_family_macro_precision` and
`pooled_macro_precision` report selected-positive fractions. `degradation` is
per-family precision minus pooled precision; positive values indicate a loss
from pooled ranking.
