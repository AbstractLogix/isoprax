# Implementation Plan: API Contract Cleanup

Decorate KB retrieval methods with `abstractmethod`, remove redundant
predeclaration aliases from the module, shim, and package exports, update
internal tests to canonical names, and return row IDs from the adequacy gate.
