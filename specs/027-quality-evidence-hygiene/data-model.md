# Data Model: Quality and Release Evidence Hygiene

The release metadata contract is the package version in `pyproject.toml` plus
the matching `## [version]` heading in `CHANGELOG.md`. Mutation scope is the
full `isoprax` package, and property evidence includes predeclaration identity
invariance under mapping insertion order.
